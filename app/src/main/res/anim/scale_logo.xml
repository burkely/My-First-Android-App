<?xml version="1.0" encoding="utf-8"?>

<!-- A container that holds other animation elements (<objectAnimator>,
<valueAnimator>, or other <set> elements). Represents an AnimatorSet.
You can specify nested <set> tags to further group animations together.
Each <set> can define its own ordering attribute. -->

<set
    xmlns:android="http://schemas.android.com/apk/res/android">

    <scale

        android:fromXScale="0.1"
        android:toXScale="1.1"
        android:fromYScale="0.1"
        android:toYScale="1.1"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="4000" />

</set>